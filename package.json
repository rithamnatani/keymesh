{
  "name": "connected-lists",
  "private": true,
  "scripts": {
    "serve": "bun run scripts/static-server.ts",
    "screenshots": "node scripts/capture-readme-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.52.0"
  }
}
